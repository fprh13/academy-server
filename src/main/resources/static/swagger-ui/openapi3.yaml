openapi: 3.0.1
info:
  title: "[라이브 클래스] 수강 신청 시스템"
  description: 수강 신청 시스템의 API 문서입니다.
  version: 2026.05.24 15:24
servers:
- url: http://localhost:8080
tags: []
paths:
  /health:
    get:
      tags:
      - "[필독] API 명세서 가이드"
      summary: API 사용법
      operationId: 서버_상태_체크_2xx
      responses:
        "200":
          description: "200"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiResponse"
              examples:
                서버_상태_체크_2xx:
                  value: |-
                    {
                      "message" : "OK",
                      "data" : null
                    }
components:
  schemas:
    ApiResponse:
      title: ApiResponse
      type: object
  securitySchemes:
    JWT:
      type: apiKey
      name: Authorization
      in: header
security:
  - JWT: []  